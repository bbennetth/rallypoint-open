// Saved storage-state file written by auth.setup.ts and reused by the
// data-heavy specs. Path is relative to the repo root (cwd when run via
// `npm run e2e`).
export const STORAGE_STATE = 'e2e/.auth/demo.json'
